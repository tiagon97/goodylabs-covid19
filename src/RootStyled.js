import styled from 'styled-components';
import ReactTooltip from 'react-tooltip';

const StyledReactTooltip = styled(ReactTooltip)`
  border-radius: 1.3rem;
  white-space: pre-wrap;
`;

export default StyledReactTooltip;
